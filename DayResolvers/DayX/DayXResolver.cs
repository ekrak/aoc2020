using System.IO;

namespace AdventOfCode2020_1.DayResolvers.DayX
{
    public class DayXResolver
    {
        public string Resolve(StreamReader input, int task)
        {
            if (task == 1) return Resolve1Internal(null);

            return Resolve2Internal(null);
        }

        private string Resolve2Internal(object input)
        {
            return null;
        }

        private string Resolve1Internal(object input)
        {
            return null;
        }
    }
}